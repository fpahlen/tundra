# Legacy: process interview path (archived)

Hard pivot (2026-08): Tundra’s active focus is an instrument-agnostic **regulation translation system**, not the business-process interview website.

This folder preserves the previous path without deleting git history:

| Path | Was |
| --- | --- |
| `web/` | Stage 1 interview FastAPI UI |
| `examples/` | Business-process demos (hours, booking, …) |
| `models/tundra-interview-session.tundra` | Dogfood model for the interview session |
| `prompts-extract-tundra.md` | Interview-oriented extract prompt |

**Do not extend this tree** for product work. New models live under repo-root `models/` and sources under `sources/`.
